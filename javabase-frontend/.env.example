# URL base da API do backend. Em dev, o fallback em src/lib/api.ts já aponta
# pra http://localhost:8080/api/v1, então este arquivo só é necessário em produção
# (ex: configurar como variável de ambiente no painel do Netlify).
VITE_API_URL=http://localhost:8080/api/v1
