package com.javabase.enums;

// Categoria de um Topic. A ordem dos valores aqui é só declarativa —
// a ordem de exibição real vem de Topic.orderIndex dentro de cada categoria.
public enum Category {
    FUNDAMENTOS,
    OOP,
    ARMADILHAS,
    COLLECTIONS,
    EXCEPTIONS,
    JAVA_MODERNO,
    ECOSSISTEMA,
    SPRING_BOOT,
    HTTP_REST,
    SQL
}
